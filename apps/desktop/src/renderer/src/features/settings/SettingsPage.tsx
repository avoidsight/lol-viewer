import type { AppSettings } from '../../../../shared/ipc';
import './settings.css';
export default function SettingsPage({ settings, message, onAutoOpenChange, onAutoAcceptChange, onLaneDifferencesChange, onClearCache }: { settings: AppSettings; message: string; onAutoOpenChange: (checked: boolean) => void; onAutoAcceptChange: (checked: boolean) => void; onLaneDifferencesChange: (checked: boolean) => void; onClearCache: () => void }) {
  return <main className="settings-page"><div className="settings-page__inner"><header className="settings-page__heading"><span>偏好设置</span><h1>设置</h1><p>管理匹配确认、对战展示和本地缓存。</p></header><section className="settings-page__section" aria-labelledby="match-settings"><h2 id="match-settings">游戏辅助</h2><label className="settings-row"><span><strong>自动打开对战信息</strong><small>检测到英雄选择或进入游戏时，自动切换到实时对局。</small></span><input type="checkbox" role="switch" checked={settings.autoOpenLiveMatch} onChange={(event) => onAutoOpenChange(event.target.checked)} /></label><label className="settings-row"><span><strong>自动接受匹配</strong><small>检测到准备确认后自动点击接受，可随时关闭。</small></span><input type="checkbox" role="switch" checked={settings.autoAcceptReadyCheck} onChange={(event) => onAutoAcceptChange(event.target.checked)} /></label><label className="settings-row"><span><strong>显示对位差异</strong><small>位置可靠时，标出与标准分路不一致的玩家。</small></span><input type="checkbox" role="switch" checked={settings.showLaneDifferences} onChange={(event) => onLaneDifferencesChange(event.target.checked)} /></label></section><section className="settings-page__section" aria-labelledby="maintenance-settings"><h2 id="maintenance-settings">本地维护</h2><div className="settings-row"><span><strong>清理缓存</strong><small>移除本地战绩缓存，下次打开时重新读取。</small></span><button type="button" onClick={onClearCache}>清理缓存</button></div></section>{message && <p className="settings-page__message" aria-live="polite">{message}</p>}</div></main>;
}
