# 战绩装备栏隐藏饰品槽

## 目标

个人战绩的装备栏不显示眼、扫描、魄罗饰品、稻草人分身等饰品。

## 设计

- LCU 对局数据中的 `item6` 是饰品槽，装备采集只读取 `item0` 至 `item5`。
- 保留现有非出装物品 ID 过滤，避免魄罗零食等特殊物品从其他槽位进入装备栏。
- 不改变装备的原始顺序，也不影响其他对局数据。

## 测试

- 回归测试在 `item6` 放入一个普通、可识别的物品 ID，断言它仍不会进入 `itemIds`。
- 完整测试、类型检查、生产构建和打包后 SQLite 冒烟测试均需通过。
